{
  "name": "shield-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end suite. Runs inside the compose network against http://web:3000.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
